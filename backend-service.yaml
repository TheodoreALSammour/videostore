apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000        # exposed inside cluster
      targetPort: 5000  # your backend container port
  type: ClusterIP
